function contador() {
    // declaração de variável
    var num = document.getElementById("num").value;

    // algoritmo
    // enquanto... faça
    while (num >= 0) {
        alert(num);
        num--;
    }
}